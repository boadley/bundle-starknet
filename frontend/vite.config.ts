// vite.config.ts - Vite configuration for development/build
// Added PWA plugin for progressive web app support (installable on mobile)
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import { VitePWA } from 'vite-plugin-pwa'

// https://vite.dev/config/
export default defineConfig({
  plugins: [
    react(),
    // PWA plugin configuration for standalone installable app
    // Generates service worker for offline support, caches assets
    // Manifest links to /manifest.json created earlier
    VitePWA({
      registerType: 'autoUpdate',
      includeAssets: ['favicon.ico', 'apple-touch-icon.png', 'masked-icon.svg'],
      manifest: {
        name: 'Bundle App - Spend Crypto in Nigeria',
        short_name: 'Bundle',
        description: 'Spend Crypto on Anything in Nigeria',
        theme_color: '#00F5D4',
        background_color: '#000000',
        display: 'standalone',
        icons: [
          {
            src: 'pwa-192x192.png',
            sizes: '192x192',
            type: 'image/png'
          },
          {
            src: 'pwa-512x512.png',
            sizes: '512x512',
            type: 'image/png'
          },
          {
            src: 'pwa-512x512.png',
            sizes: '512x512',
            type: 'image/png',
            purpose: 'maskable'
          }
        ]
      }
    })
  ],
})
